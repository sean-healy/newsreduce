> data <- read.table(file = '/home/sean/newsreduce/expirements/batch-sql-inserts/data', sep = ' ', header = FALSE); data = aggregate(data.frame(Time=data$V2), by=list(Bulk=as.factor(data$V1)), FUN=mean); data$TimePerRow <- data$Time / as.numeric(paste(data$Bulk)); data2 <- read.table(file = '/home/sean/newsreduce/expirements/batch-sql-inserts/data2', sep = ' ', header = FALSE); data2 = aggregate(data.frame(Time=data2$V2), by=list(Bulk=as.factor(data2$V1)), FUN=mean); data2$TimePerRow <- data2$Time / as.numeric(paste(data2$Bulk));
> plot(data2$Bulk, data2$TimePerRow, type="h")
